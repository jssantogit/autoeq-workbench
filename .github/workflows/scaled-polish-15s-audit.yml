name: Scaled Polish 15s Audit

on:
  push:
    branches:
      - research/audit-scaled-polish-15s-20260912

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @autoeq-workbench/core typecheck
      - name: Run 15s scaled-polish audit
        env:
          GITHUB_SHA: ${{ github.sha }}
          AUDIT_OUTPUT_DIR: ${{ github.workspace }}/autoeq-scaled-polish-15s
        run: pnpm --filter @autoeq-workbench/core exec tsx benchmarks/research/structuralAudit15.ts
      - uses: actions/upload-artifact@v4
        with:
          name: scaled-polish-15s-${{ github.run_id }}
          path: autoeq-scaled-polish-15s
          if-no-files-found: error
          retention-days: 30
