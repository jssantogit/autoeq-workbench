name: Frozen Structural Audit

on:
  push:
    branches:
      - research/audit-multiregion-20260912

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @autoeq-workbench/core typecheck
      - run: pnpm --filter @autoeq-workbench/core test -- test/autoeq/v2/structuralSearch.test.ts
      - name: Run frozen structural audit
        env:
          GITHUB_SHA: ${{ github.sha }}
          AUDIT_OUTPUT_DIR: ${{ github.workspace }}/autoeq-structural-audit
        run: pnpm --filter @autoeq-workbench/core exec tsx benchmarks/research/structuralAudit.ts
      - name: Upload frozen structural audit artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frozen-structural-audit-${{ github.run_id }}
          path: autoeq-structural-audit
          if-no-files-found: error
          retention-days: 30
