name: Near Budget Internal Feature Audit

on:
  push:
    branches:
      - research/audit-q31-x8-internal-feature-near-budget-20260912

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @autoeq-workbench/core typecheck
      - name: Run near-budget internal-feature audit
        env:
          AUDIT_OUTPUT_DIR: ${{ github.workspace }}/near-budget-internal-feature-audit
        run: pnpm --filter @autoeq-workbench/core exec tsx benchmarks/nearBudgetInternalFeatureAudit.ts
      - uses: actions/upload-artifact@v4
        with:
          name: near-budget-internal-feature-audit-${{ github.run_id }}
          path: near-budget-internal-feature-audit
          if-no-files-found: error
          retention-days: 30
