name: Polish Multiplier 15s Audit

on:
  push:
    branches:
      - research/audit-polish-x4-15s-20260912

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @autoeq-workbench/core typecheck
      - name: Run 15s polish-multiplier audit
        env:
          GITHUB_SHA: ${{ github.sha }}
          AUDIT_OUTPUT_DIR: ${{ github.workspace }}/autoeq-polish-multiplier-15s
        run: pnpm --filter @autoeq-workbench/core exec tsx benchmarks/research/structuralAudit15.ts
      - uses: actions/upload-artifact@v4
        with:
          name: polish-multiplier-15s-${{ github.run_id }}
          path: autoeq-polish-multiplier-15s
          if-no-files-found: error
          retention-days: 30
