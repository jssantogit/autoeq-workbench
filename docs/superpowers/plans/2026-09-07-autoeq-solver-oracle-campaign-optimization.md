# AutoEQ Solver Oracle Campaign Optimization Plan

> **For agentic workers:** Execute inline in this session; subagent-driven-development is explicitly prohibited by the task.

**Goal:** Reduce Oracle campaign wall-clock through case-parallel execution and staged compute while preserving the semantic, canonical-validation, and provenance contract established by `a71d3077a370408a22bea32d0ead2b36583c98c2`.

**Architecture:** The TypeScript research CLI will select one approved case deterministically and emit case-scoped control/problem artifacts. The Python lab will expose versioned stage configuration, convergence comparison, deterministic case-artifact aggregation, and adaptive escalation decisions. Continuous Oracle global candidates will be canonical-screened before Powell, while all official points remain canonically evaluated. The GitHub workflow will use a preparation job, a fail-fast-disabled case matrix with at most eight concurrent jobs, and an always-running aggregation job.

**Evidence boundary:** SCREEN/CONFIRM/DEEP are explicit nested seed-prefix stages. SMOKE is orchestration-only; FULL retains exhaustive compute. Aggregation refuses to freeze calibration for incomplete, mismatched, invalid, or vacuous evidence. No holdout data is opened and no production AutoEQ code is changed.

## Tasks

1. Add case-scoped problem/control export and tests for unknown and wrong-layer case IDs.
2. Add versioned stage configuration, stable seed-prefix selection, convergence comparison, escalation decisions, and deterministic artifact aggregation with focused Python tests.
3. Refactor Continuous Oracle scheduling so DE/CMA candidates are deduplicated and canonically screened before a deterministic Powell shortlist; record compute counts and preserve optimizer-family provenance.
4. Add case-scoped Continuous and Deliverable execution, case manifests, and aggregate artifact schemas with exact SHA/config/hash validation.
5. Replace the serial research workflow with preparation, case matrix, and aggregation jobs; expose `smoke`, `screen`, `confirm`, `deep`, and `full` modes with explicit case selection for escalation.
6. Run focused tests, local orchestration/aggregation smoke checks, then the required repository verification commands.
7. Push the Oracle branch, temporarily register the research workflow on the default branch if needed, run adversarial MaxFilters=10 SCREEN and selective CONFIRM/DEEP campaigns, record exact run/artifact evidence, and remove the temporary default-branch trigger.

## Verification

Run Python and TypeScript focused tests first. Then run the requested solver-lab tests, core research tests/typecheck, root test/typecheck/build/lint, and `git diff --check`. Inspect all generated manifests and the final diff before reporting. QTF is not started.
